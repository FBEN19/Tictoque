<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}TicToque{% endblock %}</title>
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
    
    <!-- CSS personnalisé -->
    <link rel="stylesheet" href="css/style.css">
    {% block stylesheets %}
    <style>
        .pagination a {
            color: #F4A261;
        }

        body{
            background-color:rgb(239, 242, 248) ;
        }
    </style>
    {% endblock %}
</head>
<body>

    <!-- Barre de navigation -->
    <nav class="navbar navbar-expand-lg navbar-light bg-white sticky-top shadow-sm">
        <div class="container d-flex align-items-center">

            <!-- Logo -->
            <a class="navbar-brand d-flex align-items-center" href="#">
                <img src="images/tictoque.jpg" alt="TicToque" style="height:100px;">
                <span class="ms-2 fw-bold" style="color:#F4A261;">Tictoque</span>
            </a>

            <!-- Barre de recherche -->
            <form class="d-flex flex-grow-1 mx-3">
                <input class="form-control me-2" type="search" placeholder="Je cherche..." aria-label="Search">
                <button class="btn" style="background-color : #F4A261; color:white;" type="submit">
                    <i class="bi bi-search"></i>
                </button>
            </form>
            
            <!-- Bouton Connexion -->
            <a href="./connexion.html.twig" class="btn" style="background-color : #F4A261; color:white;">
                <i class="bi bi-person"></i> Connexion
            </a>
        </div>
    </nav>

    <!-- Contenu principal -->
    <main class="container mt-4">
        {% block content %}{% endblock %}
    </main>

    <!-- Footer -->
    <footer class=" text-center py-3 mt-4" style="background-color : #F4A261; color:white;">
        <p class="mb-0">&copy; {{ "now"|date("Y") }} TicToque. Tous droits réservés.</p>
    </footer>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

    {% block javascripts %}{% endblock %}
</body>
</html>